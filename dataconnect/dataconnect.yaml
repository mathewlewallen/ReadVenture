specVersion: "v1beta"
serviceId: "readventure"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "readventure"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
